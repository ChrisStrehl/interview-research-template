# Python dependencies for the toolbox scripts (beyond stdlib urllib/json/re/etc).
# Install with: python -m pip install --user -r toolbox/requirements.txt

youtube-transcript-api   # used by youtube.py for transcript fetching
yt-dlp                   # used by youtube.py for search and as a transcript fallback
markdown                 # used by render.py to convert Markdown to HTML
pillow                   # used by render.py to downscale/recompress embedded screenshots
